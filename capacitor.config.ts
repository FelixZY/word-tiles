import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "se.fzy.wordtiles",
  appName: "Word Tiles",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
